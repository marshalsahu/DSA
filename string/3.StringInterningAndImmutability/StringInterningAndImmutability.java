public class StringInterningAndImmutability {
    public static void main(String[] args) {
        //Interning effect : make use of already present literal pool. implication: don't use == for comparison instead use .equals()
        //Immutability impacts performance and make them slow
    }
}
